function changeBorderColor(element) {
    element.style.borderColor = "var(--color4)"; // Change the border color to black on hover
}

function resetBorderColor(element) {
    element.style.borderColor = "var(--color3)"; // Reset the border color on mouseout
}
